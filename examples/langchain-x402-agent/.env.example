# Throwaway Stellar TESTNET secret only. Never a mainnet key.
STELLAR_SECRET_KEY=

# Optional overrides
NIRIUM_X402_NETWORK=stellar:testnet
NIRIUM_PREMIUM_URL=https://nirium-agent.fly.dev/api/v1/premium/signals

# Optional: use a real chat model instead of the scripted ReAct loop
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini
